#!/usr/bin/env bash
set -euo pipefail

source "$(dirname -- "$0")/vars"
echo "${navi_bin_dir}"

cat << EO_NAVI_BIN > "${navi_bin_dir}/navi"
#!/usr/bin/env bash
${navi_script_dir}/navi '$@'
EO_NAVI_BIN

chmod +x "${navi_bin_dir}/navi"
